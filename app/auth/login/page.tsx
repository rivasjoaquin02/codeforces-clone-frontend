import LoginForm from "@/components/Forms/LoginForm";

const LoginPage = () => {
    return (
        <main className="form-page">
            <LoginForm />
        </main>
    );
};

export default LoginPage;
